#include <stdio.h>

int main(){
    int n, i;
    long long dp[100];

    printf("Enter n: ");
    scanf("%d",&n);

    dp[0] = 1;

    for(i=1;i<=n;i++)
        dp[i] = i * dp[i-1];

    printf("Factorial = %lld", dp[n]);

    return 0;
}